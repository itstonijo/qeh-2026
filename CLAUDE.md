# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository (`itstonijo/qeh-2026`) is currently empty — it contains only a `.gitkeep` placeholder and a single initialization commit. There is no source code, build system, test suite, or documentation yet.

When the codebase is populated, this file should be updated to describe:

- Build, lint, test, and run commands (including how to run a single test)
- High-level architecture that spans multiple files and is not obvious from a single read
- Project-specific conventions that differ from defaults of the language/framework in use

Until then, treat conventions as TBD and confirm with the user before introducing tooling, frameworks, or directory layouts.
