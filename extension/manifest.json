{
  "manifest_version": 3,
  "name": "DOM Scrobe",
  "version": "1.1.0",
  "description": "Extract links, text, tables, and structured metadata from any web page (or a batch of URLs) and download it as JSON or CSV.",
  "permissions": ["activeTab", "scripting", "downloads", "tabs"],
  "host_permissions": ["<all_urls>"],
  "action": {
    "default_popup": "popup.html",
    "default_title": "DOM Scrobe — extract page data"
  }
}
